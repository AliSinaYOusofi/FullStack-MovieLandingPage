import React, { useEffect, useRef } from 'react';
// other functionlaites should be implementd here
// only onClick of delete in parent comp should be handled in parent comp
import gsap from 'gsap';
import axios from 'axios';

const DeleteUser = ({user_email, show}) => {
    let alertDeleteRef = useRef();

   

    

    

    return (
        <>
            
        </>
    );
}

export default DeleteUser;